plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.microsoft.playwright', name: 'playwright', version: '0.180.0'
    compile group: 'org.testng', name: 'testng', version: '+'
}

test {
    useTestNG()
}